name = "moona-rituels"
main = "index.js"
compatibility_date = "2024-10-01"
compatibility_flags = ["nodejs_compat"]

# Stores supportés : moona-9413.myshopify.com, andromeda-paris.myshopify.com
# L'identification du store se fait via l'Origin de la requête (voir index.js)

# Secrets à configurer via CLI (jamais dans ce fichier) :
#   wrangler secret put MOONA_ADMIN_API_TOKEN
#   wrangler secret put ANDROMEDA_ADMIN_API_TOKEN
